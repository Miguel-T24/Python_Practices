# 30. Write a ptyhon program that will accept the ase and height of a triangle and compute its area

base = float(input("Enter base: "))
height = float(input("Enter height: "))

print("The area of the triangle is {:.2f}".format((base * height) / 2))